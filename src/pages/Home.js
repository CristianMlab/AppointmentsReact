import { Link, Outlet } from 'react-router-dom';
import Navbar from '../Components/Navbar';

const Home = () => {
    return (
        <>
            <h1>This is a simple web app for keeping track of contacts and appointments.</h1>
            <h2>Built using React (with React Router 6)</h2>
        </>
    )
}

export default Home;